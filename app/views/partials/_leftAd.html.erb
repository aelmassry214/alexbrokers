      <% @leftAd.each do |leftAd|%>
             <% if leftAd.assets.exists? %>
							<div class='margin-ads'>
								<%= link_to image_tag(leftAd.assets.first.image.url(:thumb), class:"generalAds-Image"), leftAd %>
								<p><%= leftAd.name %></p>
							</div>
						<% else %>
							<div class='margin-ads'>
								<%= link_to image_tag('/images/genImage_thumb.jpg', class:'generalAds-Image'), leftAd%>
								<p><%= leftAd.name %></p>
            	</div>
            <% end %>
      <% end %>

      <% if @leftAd.length < 5 %>
          <% generalAdsLength = 5-@leftAd.length %>
          <% for generalAds in 1..generalAdsLength %>
             	<div class='margin-ads'>
              	<%=	link_to image_tag('/images/alexbrokers4.jpg', class:'generalAds-Image'), about_path %>
              	<p>اتصل بنا</p>
              </div>
          <% end %>
      <% end %>